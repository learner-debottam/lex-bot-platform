{
  "name": "lambdas",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "package": "npm run clean && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}